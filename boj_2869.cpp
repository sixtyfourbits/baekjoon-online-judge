#include <iostream>
#include <algorithm>
#include <math.h>

using namespace std;

int main()
{
    int a, b, v;
    cin >> a >> b >> v;
    cout << ceil((float)(v-a) / (float)(a-b)) + 1;

    // 퍼센트 연산자
}